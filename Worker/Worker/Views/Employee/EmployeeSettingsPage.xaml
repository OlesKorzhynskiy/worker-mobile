<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Worker.Views.Employee.EmployeeSettingsPage"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="{StaticResource StandartPagePadding}" Spacing="0">
                <StackLayout Orientation="Horizontal" Spacing="0">
                    <Label Text="Зробити мій акаунт невидимий для нових вакансій: " TextColor="{StaticResource TextColor}" FontSize="Small" VerticalOptions="CenterAndExpand" HorizontalOptions="Start" />
                    <Switch IsToggled="{Binding IsVisible}" VerticalOptions="CenterAndExpand" HorizontalOptions="Start"/>
                </StackLayout>

                <Label Text="Електронна пошта:" TextColor="{StaticResource TextColor}" FontSize="Small" Margin="0, 20, 0, 0"/>
                <Entry Placeholder="Електронна пошта" Text="{Binding Email}" TextColor="{StaticResource TextColor}" FontSize="Small" />
                <Button Text="Оновити" FontSize="Small" Command="{Binding UpdateEmailCommand}" TextColor="{StaticResource TextColor}" HorizontalOptions="Center" HeightRequest="40"/>

                <Label Text="Номер телефону:" TextColor="{StaticResource TextColor}" FontSize="Small" Margin="0, 20, 0, 0"/>
                <Entry Placeholder="Телефон" Keyboard="Telephone" Text="{Binding Phone}" TextColor="{StaticResource TextColor}" FontSize="Small" />
                <Button Text="Оновити" FontSize="Small" Command="{Binding UpdatePhoneCommand}" TextColor="{StaticResource TextColor}" HorizontalOptions="Center" HeightRequest="40"/>

                <Label Text="Пароль:" TextColor="{StaticResource TextColor}" FontSize="Small" Margin="0, 20, 0, 0"/>
                <Entry Placeholder="Старий пароль" IsPassword="True" Text="{Binding OldPassword}" TextColor="{StaticResource TextColor}" FontSize="Small" />
                <Entry Placeholder="Новий пароль" IsPassword="True" Text="{Binding NewPassword}" TextColor="{StaticResource TextColor}" FontSize="Small" />
                <Entry Placeholder="Повторіть новий пароль" IsPassword="True" Text="{Binding ConfirmNewPassword}" TextColor="{StaticResource TextColor}" FontSize="Small" />
                <Button Text="Оновити" FontSize="Small" Command="{Binding UpdatePasswordCommand}" TextColor="{StaticResource TextColor}" HorizontalOptions="Center" HeightRequest="40"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>